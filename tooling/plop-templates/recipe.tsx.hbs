import React from "react";

import Page from "components/Page";
import Post from "components/Post";

import Text from "components/Text";
import Ingredients from "components/Ingredients";

import heroImage from "public/images/{{categoryPath}}/{{slug}}/thumbnails/tall.png";
import metaImage from "public/images/{{categoryPath}}/{{slug}}/thumbnails/meta.png";

import Method from "components/Method";

const INGREDIENTS = [
  {
    value: 182,
    label: "Example",
  },
];

const STEPS = [<Text>Add steps here</Text>];

export default function {{pascalCase title}}() {
  return (
    <Page
      metaTitle="{{title}}"
      description="{{description}}"
      image={metaImage}
    >
      <Post
        title="{{title}}"
        heroImage={heroImage}
        ingredients={
          <Ingredients ingredientSuffix="update me" ingredients={INGREDIENTS} />
        }
        method={<Method recipeMethod={STEPS} />}
        description={
          <>
            <Text variant="ingress">{{description}}</Text>
          </>
        }
      ></Post>
    </Page>
  );
}
